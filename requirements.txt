# Streamlit (pin to a stable version compatible with Python 3.12)
streamlit==1.40.0

# Data libraries - pin versions compatible with Python 3.12 and each other
pandas==2.2.3
numpy==1.26.4

# HTTP requests
requests==2.32.3

# Optional dependencies used by pandas/streamlit for better performance
pyarrow==17.0.0
protobuf==4.25.5
